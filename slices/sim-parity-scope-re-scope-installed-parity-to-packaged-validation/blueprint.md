# Blueprint

## Plan

1. Narrow parity validation semantics to packaged installs.
2. Preserve source-linked local install workflow as an explicit, separate mode.
3. Update maintenance-facing guidance so reporting reflects the same boundary.

## Validation

- confirm maintenance/docs language references packaged parity rather than all installs
- confirm closed slice metadata remains durable after the documentation repair
