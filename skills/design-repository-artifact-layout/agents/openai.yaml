interface:
  display_name: "Design Repository Artifact Layout"
  short_description: "Choose minimal canonical homes for technical artifacts"
  default_prompt: "Use $design-repository-artifact-layout to recommend the smallest lifecycle-aware canonical home for justified artifacts, preserving local conventions or deriving a minimal layout when no usable guide exists."
