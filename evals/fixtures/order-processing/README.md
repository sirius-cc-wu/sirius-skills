# Order Processing Fixture

This fixture represents a small existing service. A reviewer needs to
understand its module dependencies and the runtime path for placing an order
without reading every source file. The requested architecture note is an
as-built view at the fixture snapshot, not a statement of intended future
architecture.
