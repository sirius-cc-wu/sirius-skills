# Implementation Slice Briefing Fixture

This synthetic repository contains approved requirements and examples, a
black-box use case, one approved design boundary, and a minimal current codebase.
Opaque-reference matching is ready; queue persistence is not because retention
has no approved rule.

Briefs belong under `briefs/`. Requirements, analysis, design, production code,
tests, and repository guidance must remain unchanged.
