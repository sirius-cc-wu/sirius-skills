# Host-Safe Rust Refactoring Fixture

This synthetic Rust fixture represents a partially completed validation
refactoring. Focused tests protect two local seams, but the approved parent
outcome remains open because no representative end-to-end validation flow owns
and proves the complete runtime boundary.

The next iteration is design-only. Update the existing design owner before
changing Rust source or tests.
