{
  "skill_name": "behavior-preserving-refactoring",
  "trigger": {
    "positive": [
      {
        "prompt": "Consolidate these scattered environment reads into the existing typed configuration owner behind current passing tests without moving a system boundary",
        "top_k": 3
      },
      {
        "prompt": "Code review found orchestration in the wrong module; move it to the existing composition owner while preserving behavior, ownership, errors, ordering, and concurrency",
        "top_k": 3
      },
      {
        "prompt": "Move this cohesive calculation to the existing module that already owns its data, then rerun focused and regression checks",
        "top_k": 3
      }
    ],
    "negative": [
      {
        "prompt": "Refactor the Rust validation environment by introducing a backend, composition root, service entrypoints, task supervision, and a new end-to-end test boundary",
        "owner": "iterative-risk-driven-development"
      },
      {
        "prompt": "Design exact Rust resource transfer, startup rollback, async cancellation, joining, Drop fallback, and fallible cleanup without implementation",
        "owner": "design-rust-lifecycles"
      }
    ]
  },
  "evals": [
    {
      "id": "local-verified-transformation",
      "prompt": "Behind the current green tests, move one cohesive calculation to the existing domain module that owns its data and correct the dependency direction. Preserve behavior, ownership, errors, ordering, and concurrency; rerun focused and broader checks; and update durable design knowledge only if its represented ownership changes.",
      "expected_output": "One behavior-preserving responsibility and dependency transformation improves the named structural design pressure and remains protected by focused and regression evidence.",
      "expectations": [
        "The observable invariant and green baseline are explicit",
        "One responsibility or dependency pressure bounds the transformation",
        "Focused checks run after the transformation and broader checks remain green",
        "No boundary-sensitive system redesign is hidden inside the refactoring"
      ],
      "prohibitions": [
        "Do not change required behavior or weaken a valid test",
        "Do not expand into routine naming, extraction, duplication, or control-flow cleanup",
        "Do not create a new backend, composition root, runtime owner, or test boundary as a local refactoring",
        "Do not create a standalone refactoring record without an independent need"
      ],
      "allowed_mutations": [
        "src/**",
        "tests/**"
      ],
      "trust_level": "provisional"
    }
  ]
}
