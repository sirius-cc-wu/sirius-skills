# Brief

## Goal

Re-scope installed parity validation so packaged installs remain the supported
parity target while source-linked local development setup is treated as a
separate workflow.

## Acceptance

- maintenance validation no longer treats local source-linked setup as packaged
  parity drift
- packaged-install parity checks continue to guard the intended supported path
