# Blueprint

- Add typed continuation policy parsing for `review_boundary` and `commit_checkpoint`.
- Preserve hard-stop guardrails while allowing policy-driven continuation where eligible.
- Validate behavior with ship and ship-slice tests.
